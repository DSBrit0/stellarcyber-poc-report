# Porta onde o servidor irá escutar
PORT=8080

# IP de escuta:
#   0.0.0.0  → aceita conexões de qualquer IP (recomendado para produção atrás de firewall/nginx)
#   127.0.0.1 → somente localhost
#   <ip>     → bind em interface específica (ex: 10.0.0.5)
HOST=0.0.0.0
